<!-- Image and text -->
<nav class="navbar navbar-light bg-dark   navbar-inverse fixed-top">

  <%= link_to "Trading1", dashboard_index_path, class: "navbar-brand text-white" %>

  <span>
  	<p class="navbar-text pull-right text-white">
    <% if user_signed_in? %>
      <strong class="badge badge-pill badge-dark"><%= (current_user.first_name + " " + current_user.last_name) %></strong>
      <%= link_to 'Edit Profile', edit_user_registration_path, :class => 'navbar-link text-white' %> |
      <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link text-white corner-text'  %>
      <%= image_tag(current_user.user_image, class: "image") %>

    <% end %>
    </p>
  </span>
</nav>

<p class="notice"><% notice %></p>
<p class="alert"><% alert %></p>
</nav>